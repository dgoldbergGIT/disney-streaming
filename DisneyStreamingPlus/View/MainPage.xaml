<Page
    x:Class="DisneyStreamingPlus.MainPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:local="using:DisneyStreamingPlus.ViewModel"
	d:DataContext="{d:DesignInstance Type=local:MainPageViewModel}"
	mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

	<Page.Resources>
		<DataTemplate x:Key="imagesTemplate">
			<StackPanel Orientation="Vertical">
				<TextBlock Text="{Binding Caption}" FontSize="30" FontWeight="Bold" />
				<GridView ItemsSource="{Binding ImageUrls}">
					<ItemsControl.ItemsPanel>
						<ItemsPanelTemplate>
							<StackPanel Orientation="Horizontal" />
						</ItemsPanelTemplate>
					</ItemsControl.ItemsPanel>
					<ItemsControl.ItemTemplate>
						<DataTemplate>
							<Image Source="{Binding}" Height="125" Width="125" />
						</DataTemplate>
					</ItemsControl.ItemTemplate>
				</GridView>
			</StackPanel>
		</DataTemplate>

		<DataTemplate x:Key="rowHeader">
			<TextBlock Text="{Binding Path=Caption}" FontSize="30" FontWeight="Bold" />
		</DataTemplate>
	</Page.Resources>

	<Grid>
		<ScrollViewer HorizontalScrollBarVisibility="Auto">
			<GridView ItemsSource="{Binding Rows}" ItemTemplate="{StaticResource imagesTemplate}">
				<!--<GridView.ItemsPanel>
				<ItemsPanelTemplate>
					<ItemsWrapGrid Orientation="Horizontal"></ItemsWrapGrid>
				</ItemsPanelTemplate>
			</GridView.ItemsPanel>-->
			</GridView>
		</ScrollViewer>
	</Grid>
</Page>